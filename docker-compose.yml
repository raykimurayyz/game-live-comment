services:
  gamelivecomment:
    build: .
    container_name: gamelivecomment
    restart: unless-stopped
    ports:
      - "3010:3010"
      - "6667:6667"
    volumes:
      - gamelivecomment-data:/app/data
    environment:
      NODE_ENV: production

volumes:
  gamelivecomment-data:
